# =============================================================================
#  vmarker 环境变量配置示例
# =============================================================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# AI 聊天配置 (用于章节分段、大纲生成、字幕润色)
# -----------------------------------------------------------------------------

# OpenAI API Key 或兼容服务的密钥
# 获取方式: https://platform.openai.com/api-keys
API_KEY=sk-xxx

# API 基础 URL (支持 OpenAI 兼容服务)
# - OpenAI: https://api.openai.com/v1
# - Azure: https://your-resource.openai.azure.com/openai/deployments/your-deployment
# - 其他兼容服务: 根据服务商提供
API_BASE=https://api.openai.com/v1

# 使用的模型名称
# - gpt-4o-mini (推荐，性价比高)
# - gpt-4o (更强但更贵)
# - gpt-3.5-turbo (旧版)
API_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# ASR 语音识别配置 (可选)
# -----------------------------------------------------------------------------

# ASR API 基础 URL (默认与 API_BASE 相同)
# 如需单独配置 ASR 服务，取消注释并设置
# ASR_API_BASE=https://api.openai.com/v1

# ASR 模型名称 (默认: whisper-1)
# ASR_MODEL=whisper-1

# -----------------------------------------------------------------------------
# 开发配置
# -----------------------------------------------------------------------------

# 后端服务端口 (默认: 8000)
# PORT=8000

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
